# This workflow will build a golang project
# For more information see: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-go

name: Go build and publish artifact

on:
  push:
    tags:        
      - 'v[0-9]+.[0-9]+.[0-9]+'
      - 'v[0-9]+.[0-9]+.[0-9]+-rc[0-9]+'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4

    - name: Set up Go
      uses: actions/setup-go@v4
      with:
        go-version: '1.23.2'

    - name: Extract tag from the reference
      run: |
        SKUARE_GIT_TAG=$(echo $GITHUB_REF | sed 's/refs\/tags\///')
        echo "Tag name is $SKUARE_GIT_TAG"
        echo "SKUARE_GIT_TAG=$SKUARE_GIT_TAG" >> $GITHUB_ENV
    
    - name: Build with tag
      run: go build -a -installsuffix cgo -o app .

    - name: Upload artifact with tag
      uses: actions/upload-artifact@v4
      with:
        name: service-mesh-${{ env.SKUARE_GIT_TAG }}
        path: ${{ github.workspace }}/app

    - name: Configure Docker for insecure registry
      run: |
        echo '{ "insecure-registries" : ["nexuszolara.me"] }' | sudo tee /etc/docker/daemon.json
        sudo systemctl restart docker

    - name: Create Harbor TLS certificate and key
      run: |
        echo "${{ secrets.HARBOR_CERT }}" > /tmp/harbor.crt
        echo "${{ secrets.HARBOR_KEY }}" > /tmp/harbor.key
        
        sudo cp /tmp/harbor.crt /usr/local/share/ca-certificates/harbor.crt
        sudo update-ca-certificates

    - name: Login to Harbor
      uses: docker/login-action@v3
      with:
        registry: "nexuszolara.me"
        username: ${{ secrets.HARBOR_USERNAME  }}
        password: ${{ secrets.HARBOR_PASSWORD }}

    - uses: ko-build/setup-ko@v0.8
    - name: Build and push Docker image using ko and .ko.yaml
      env:
        KO_DOCKER_REPO: nexuszolara.me/skuare-proxy
        GODEBUG: x509ignoreCN=1
      run: |
        ko login nexuszolara.me --username ${{ secrets.HARBOR_USERNAME }} --password ${{ secrets.HARBOR_PASSWORD }}
        # ko build -t ${{ env.SKUARE_GIT_TAG }} .
        ./build-ko.sh -t ${{ env.SKUARE_GIT_TAG }}

    - name: Push to Harbor
      run: |
        echo "Pushing the image to Harbor"
        docker tag ko.local/service-mesh:${{ env.SKUARE_GIT_TAG }} nexuszolara.me/skuare-proxy/service-mesh:${{ env.SKUARE_GIT_TAG }}
        docker push nexuszolara.me/skuare-proxy/service-mesh:${{ env.SKUARE_GIT_TAG }}

        echo "Pushing the Helm chart to Harbor"
        sed -i 's#SKUARE_GIT_TAG#${{ env.SKUARE_GIT_TAG }}#g' helm/service-mesh/Chart.yaml
        sed -i 's#SKUARE_GIT_TAG#${{ env.SKUARE_GIT_TAG }}#g' helm/service-mesh/values.yaml
        helm package helm/service-mesh
        HELM_EXPERIMENTAL_OCI=1 helm push service-mesh-helm-${{ env.SKUARE_GIT_TAG }}.tgz oci://nexuszolara.me/skuare-proxy